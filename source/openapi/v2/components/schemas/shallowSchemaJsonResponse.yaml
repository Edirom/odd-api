allOf:
  - type: object
    properties:
      data:
        type: array
        items:
          type: object
          allOf:
            - type: object
              properties:
                type:
                  type: string
                  default: schemas
                id:
                  type: string
                  description: The unique identifier as required by the JSON:API specification
                attributes:
                  type: object
                  properties:
                    ident:
                      type: string
                      description: The identifier for the schema (e.g. "tei_all")
                    versions:
                      type: array
                      description: The available versions of this schema (e.g. "3.3.0")
                      items:
                        type: string
            - $ref: ./selfLink.yaml
  - $ref: ./selfLink.yaml
